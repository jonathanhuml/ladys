dataset:
  name: area2_bump
  split: val
  bin_size_ms: 5
  max_trials: null

model:
  name: mint
  objective: mint_likelihood_recursion
  dataset: area2_bump
  train_source: h5
  eval_bin_size_ms: 5
  n_candidates: 2
  window_length: 100
  delta: 4
  sigma: 5
  min_rate: 0.1
  causal: false
  optimization:
    name: library_fit

preprocessing:
  observations: null

trainer:
  epochs: 1
  batch_size: 1
  device: cpu

experiment:
  output_dir: runs
  run_name: mint_area2_bump_nlb_5ms
  save_predictions: true
