dataset:
  name: mc_rtt
  split: val
  bin_size_ms: 5
  max_trials: null

model:
  name: mint
  objective: mint_likelihood_recursion
  dataset: mc_rtt
  train_source: lfads
  eval_bin_size_ms: 5
  n_candidates: 5
  window_length: 100
  delta: 4
  sigma: 0
  min_rate: 0.1
  causal: false
  lfads_epochs: 100
  lfads_batch_size: 64
  lfads_seed: 0
  optimization:
    name: library_fit

preprocessing:
  observations: null

trainer:
  epochs: 100
  batch_size: 1
  device: cpu

experiment:
  output_dir: runs
  run_name: mint_mc_rtt_nlb_5ms
  save_predictions: true
