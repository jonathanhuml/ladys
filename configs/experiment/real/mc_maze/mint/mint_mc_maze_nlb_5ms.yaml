dataset:
  name: mc_maze
  split: val
  bin_size_ms: 5
  max_trials: null

model:
  name: mint
  objective: mint_likelihood_recursion
  dataset: mc_maze
  train_source: h5
  eval_bin_size_ms: 5
  n_candidates: 2
  window_length: 100
  delta: 4
  sigma: 6
  min_rate: 0.1
  causal: false
  optimization:
    name: library_fit

preprocessing:
  observations: null

trainer:
  epochs: 0
  batch_size: 1
  device: cpu

experiment:
  output_dir: runs
  run_name: mint_mc_maze_nlb_5ms
  save_predictions: true
